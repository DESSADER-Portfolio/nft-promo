extends /templates/common.pug
include _blocks/blocks.pug

block vars
  - const content = require("@src/content/index.json");
  - const pageClass = "index-page";

block title
  title #{appData.name} – #{content.title}
  
block content

  // Start: [intro-section] 
  +introSection({
    heading: content.intro.heading,
    button: content.intro.button,
    description: content.intro.description,
    counter: content.intro.counter
  })
  // End: [intro-section] 