mixin typography(props = {})
  - const availableElements = ["p", "span", "h1", "h2", "h3", "h4", "h5", "h6"];
  - const variants = ["caption", "body-small", "body-default", "body-large", "h1", "h2", "h3", "h4"];
  - const weights = ["light", "regular", "medium", "semibold", "bold"];

  - const rootClassName = "typography";
  - const className = props.class || null;
  - const style = props.style || null;
  - const as = availableElements.includes(props.as) ? props.as : availableElements[0];
  - const variant = variants.includes(props.variant) ? props.variant : variants[0];
  - const weight = weights.includes(props.weight) ? props.weight : null;
  -
    const classes = [
      rootClassName,
      {[`${rootClassName}:variant-${variant}`]: variant},
      {[`${rootClassName}:weight-${weight}`]: weight},
      className
    ];

  #{as}(
    class = classes
    style = style
  )
    block